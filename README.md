# Python-Bot-FaceBook-Graph-API
This code uses Facebook's Graph API to retrieve data from a specified Facebook group, including posts and comments. The data is then stored in a CSV file. The necessary Facebook app credentials and user access token need to be provided for the code to work correctly.
